<!DOCTYPE html>
<html lang="en">
<head>
<% include ../views/_Head %>
  <script type="text/javascript" src="/scripts/index_app.js"></script>
</head>
<body>
  <header>
  <% include ../views/_Header %>
  </header>
    <main>
    <h3>Here be text</h3>
    <section class="container">
    <% for (i = 0; i < resources.length; i++) {%>
    <%=  `${resources[i].id} ${resources[i].url} ${resources[i].title} ${resources[i].description} \n`%>
    <%} %>
    <% %>
    </section>
  </main>
  </body>
</html>